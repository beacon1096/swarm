# Secrets
*.pub
*.key
*.decrypted~*.yaml
/age.key
/cloudflare-tunnel.json
/github-deploy.key
/github-deploy.key.pub
/github-push-token.txt
# Template config files
/cluster.yaml
/nodes.yaml
# Kubernetes
kubeconfig
talosconfig
# talhelper genconfig output: contains machineconfig with cluster CA/keys/tokens.
# talhelper auto-generates a per-dir .gitignore, but make it explicit here too.
/talos/clusterconfig/
# Misc.
.private/
.task/
.venv/
.DS_Store
Thumbs.db
