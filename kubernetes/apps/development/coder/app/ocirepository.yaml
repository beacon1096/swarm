---
# Pulled from `https://helm.coder.com/v2`:
#   helm pull coder-v2/coder --version 2.31.2 -d /tmp
#   # the helm CLI saves it as `coder_helm_2.31.2.tgz` (note underscore-helm)
#   helm push /tmp/coder_helm_2.31.2.tgz oci://zot.registry.svc.cluster.local:5000/charts --plain-http
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: coder
spec:
  interval: 1h
  url: oci://zot.registry.svc.cluster.local:5000/charts/coder
  ref:
    tag: 2.31.2
  insecure: true
