---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: openstatus-dashboard
  labels:
    app.kubernetes.io/name: openstatus-dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: openstatus-dashboard
  template:
    metadata:
      labels:
        app.kubernetes.io/name: openstatus-dashboard
    spec:
      containers:
        - name: dashboard
          image: 172.16.87.51:5000/openstatushq/openstatus-dashboard:ef7691a
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: openstatus-config
            - secretRef:
                name: openstatus-secret
          env:
            - name: OPENSTATUS_CONFIG_REVISION
              value: "2026-05-15-1"
            - name: PORT
              value: "3000"
          ports:
            - name: http
              containerPort: 3000
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 45
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 90
            periodSeconds: 30
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: openstatus-dashboard
spec:
  selector:
    app.kubernetes.io/name: openstatus-dashboard
  ports:
    - name: http
      port: 3000
      targetPort: http
