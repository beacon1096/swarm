---
# Tailscale's helm chart isn't published to OCI; pull from the
# upstream HTTP helm repo (same exception as Longhorn).
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: tailscale
spec:
  interval: 1h
  url: https://pkgs.tailscale.com/helmcharts
