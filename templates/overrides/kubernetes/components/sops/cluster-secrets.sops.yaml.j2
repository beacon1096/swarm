---
# Override of templates/config/kubernetes/components/sops/cluster-secrets.sops.yaml.j2
# adds the locally-introduced secret variables that aren't part of
# cluster-template's stock list.
#
# Add new ${SECRET_*} substitutions here as we land them.
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
stringData:
  SECRET_DOMAIN: "#{ cloudflare_domain }#"
  # Tailscale operator OAuth client (same client across both
  # talos-ii and talos-i since they share a tailnet).
  SECRET_TAILSCALE_OAUTH_CLIENT_ID: kjnW4ZGxQi11CNTRL
  SECRET_TAILSCALE_OAUTH_CLIENT_SECRET: tskey-client-kjnW4ZGxQi11CNTRL-JDDQehteC49LNPSkdprE49SYVeGHV8qg
  # matrix-synapse — substituted into helmrelease.yaml's
  # `extraSecrets.oidc_providers[].client_id/client_secret` and
  # `extraSecrets.registration_shared_secret`. The OIDC values
  # match the row in authentik's restored DB; the registration
  # shared secret was carried over from swarm-01 verbatim.
  MATRIX_REGISTRATION_SHARED_SECRET: d7f3105453debd1e3ea88845de4c3d01190b902940f8aefb31bd4e7b4ebd807c
  MATRIX_OIDC_CLIENT_ID: ECe7Lng2YKh13UTZYPdjMyrBy3IxXuSfZUj5YFDp
  MATRIX_OIDC_CLIENT_SECRET: "C!F^dz2A6I21XX%OmGy9O@D%lAlifS24"
